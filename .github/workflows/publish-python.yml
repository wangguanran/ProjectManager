name: Publish Python Package to PyPI

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Require stable publish from main
        shell: bash
        run: |
          set -euo pipefail
          if [ "${GITHUB_REF_NAME}" != "main" ]; then
            echo "::error::Stable PyPI publish must run from the main branch."
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        env:
          PROJMAN_RELEASE_CHANNEL: stable
        run: |
          python scripts/write_build_info.py
          python -m build

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          python -m twine upload --repository-url https://upload.pypi.org/legacy/ dist/*

      # - name: Publish to GitHub Package Registry
      #   env:
      #     TWINE_USERNAME: ${{ github.actor }}
      #     TWINE_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
      #   run: |
      #     python -m twine upload --repository-url https://pypi.pkg.github.com/${{ github.repository_owner }}/ dist/* 
